name: three_dimensional_viewer
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_gl: ^0.0.21
  flutter_hooks: ^0.20.3
  three_dart: ^0.0.16
  three_dart_jsm: ^0.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
